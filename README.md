# Example project for testing the implementation of various authentication providers in ReactNative

## Google Single Sign In

Tried implementing it through a WebView in ReactNative but Google does not allow this way, it reponds with "user agent not allowed".
